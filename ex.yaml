

targets:

  today:
    args: {$format: "+%D"}
    body:
      - shell: date ${format}
      - var:
          $hello:
            tourist: youare
      - call:
          dRun:
            $image: "hey"
            $cmd: "ok"
            $runner: "k8s"
