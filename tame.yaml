

targets:
  build:
    args: {$imageName: "myimg", $tag: "latest"}
    body:
      - shell: echo image is ${imageName}
        opts: [silent]
        out: $my_docker_output
      - shell: |
          echo output is ${my_docker_output}
        opts: [silent]
      - call:
          echo: {$msg: "WOKRS"}

  echo:
    args: {$msg: hello }
    body:
    - shell: "echo MSG: ${msg}"
