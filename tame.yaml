

targets:
  today:
    args: {$format: "+%D"}
    body:
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}
      - shell: date ${format}

  loop-me:
    body:
      - shell: |
          while true; do
            echo "About 90.200.000 results (0,59 seconds)"
          done
