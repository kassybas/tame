
settings:
  init: "set -eu"

targets:

  # echo:
  #   args: {$msg: ~}
  #   body:
  #     - shell: echo my msg $msg
  #       out: $res
  #   return: $res

  # name:
  #   body:
  #     - call:
  #         echo: {$msg: barna}
  #       result: $echores
  #   return: "$echores"

  mapme:
    body:
      - var:
          $my:
            lovely: "is amazing"
            again: "somth"
            deeper:
              how: "deepisyour"
              listing:
                - "one"
                - "two"
      - shell: echo $my_lovely
      - shell: echo $my_again
      - shell: echo $my_deeper_how


  # yolo:
  #   body:
  #     - var:
  #         $myvar: "lovelyday"
  #         $other: "notso good"
  #         $and: "aaaand"
  #     - shell: echo variable is $myvar
  #     - shell: echo and is $other
  #     - shell: echo $and
