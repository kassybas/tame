

targets:

  build:
    .opts: silent
    .summary: "Builds something"
    .args: { foo: "world", nope }
    .body:
      - say-hello:
          msg: "hello"
        .opts: silent
        .out: myVar
        .err: myErrvar
        .rc: myRcvar
      - .exec: |
          echo hello
