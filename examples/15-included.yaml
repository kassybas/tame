settings:
  shieldEnv: true
  shell: /bin/sh

targets:

  Caller:
    run:
    - sh: echo "i will call a different target"
    - call called: {}

  Log:
    args: {$msg: "message"}
    run:
    - sh: echo Shell is $0
    - sh: date "+%T"
      $: $timestamp
      opts: silent
    - sh: printf "[%s] %s\n" "${timestamp}" "${msg}"

  Called:
    run:
    - sh: echo "i was Called"
      $: $result
    - return: $result