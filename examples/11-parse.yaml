

settings:
  shieldEnv: true

globals:
  $tags:
  - latest
  - 2020.01
  - release

# tame -f examples/10-for.yaml Builder
targets:
  Echo:
    args: {$msg: "OK"}
    run:
    - var $msg: "changed!!!!" 
    # - var $okok: "nope" ## TODO: fix this is not working
    - sh: echo $msg

  Caller:
    run:
    - call Echo: {$msg: "yoyoyo"}
